# Kayan Marketing OS — Backend

## Setup
1. `npm install`
2. Copy `.env.example` to `.env` and fill values
3. Install Supabase CLI: `npm install -g supabase`
4. Run migrations: `supabase db push`

## Standards
See `CLAUDE.md`.
